# Data placeholder

Do not store raw or derived participant-level China Kadoorie Biobank (CKB) data in the public repository.

This folder is only a placeholder to document the expected input files:

- `baseline_data.RData` containing object `baseline_data`
- `events_data.RData` containing object `events_data`

If you need to demonstrate the workflow publicly, use a small synthetic example dataset that does not contain any real participant data.
