# Data placeholder

Do not upload restricted individual-level data to the public repository.

This folder is a placeholder only. If the analysis uses protected biobank or hospital data, keep the raw data outside the Git repository and document the access procedure in the manuscript.

Suggested local input:

- `analysis_input.RData`

This file may contain project-specific objects such as:

- `df_filtered`
- `data1`
- `M.names1`
- `X.names1`
- `y.names`
